{
    "name": "sg-transport",
    "version": "1.0.0",
    "engines": {
        "node": ">=18"
    },
    "description": "🇸🇬 NodeJS wrapper for easy integration with Singapore's real-time transport data.gov.sg APIs",
    "main": "index.js",
    "scripts": {
        "build": "tsc",
        "test": "jest --forceExit --runInBand",
        "lint": "lint-staged --config lint-staged.js",
        "format": "prettier --write .",
        "prettier": "prettier src -check",
        "prepare": "husky install",
        "start": "yarn build && node bin/trade-stream-aggregator.js"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/jest": "^29.5.11",
        "husky": "^8.0.3",
        "jest": "^29.7.0",
        "lint-staged": "^15.2.0",
        "prettier": "^3.1.1",
        "ts-jest": "^29.1.1",
        "tsc-files": "^1.1.4",
        "typescript": "^5.3.3"
    },
    "jest": {
        "testEnvironment": "node",
        "transform": {
            "\\.(ts|tsx)?$": "ts-jest"
        }
    }
}
